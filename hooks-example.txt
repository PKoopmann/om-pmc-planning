# Example hook definitions

PREFIX : <http://om-pmc.example.de#>
PREFIX other: <http://om-pmc.example.de#>

server1_overloaded := ::OverloadedServer(:other:server1)
server2_overloaded := ::OverloadedServer(:other:server2)

