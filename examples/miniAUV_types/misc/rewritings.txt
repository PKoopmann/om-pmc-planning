    inconsistent = false

    KBquery_canTakePicture ?x =
        if (inconsistent)
             then true
        if ((?x == remaroAUV))
            then ((canTakePicture rgbCamera))
    else
        false;


    KBquery_canDrive ?x =
        if (inconsistent)
             then true
        if ((?x == remaroAUV))
            then ((canDrive backThruster))
    else
        false;


